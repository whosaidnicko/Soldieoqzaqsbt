//
//  Uispqpbirqwr.swift
//  Soldieoqzaqsbt
//
//  Created by Nicolae Chivriga on 18/02/2025.
//

import SwiftUI

struct Uispqpbirqwr: View {
    @Environment(\.dismiss) var dismiss
    var body: some View {
        ZStack {
            Image("soqi9nshq")
                .resizable()
                .ignoresSafeArea()
            
            Color.black.opacity(0.75)
                .ignoresSafeArea()
            
            Image("kukloasbnujtg")
                .resizable()
                .scaledToFit()
                .padding(40)
        }
        .navigationBarBackButtonHidden()
        .navigationBarItems(leading: Button(action: {
            dismiss()
        }, label: {
            Image("builqpzibutow")
        }))
    }
}
